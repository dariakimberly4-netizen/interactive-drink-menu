line-height:1.55;margin:0 auto 18px;max-width:430px}
  .expired-meta{
    display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:18px 0
  }
  .expired-meta div{
    border:1px solid var(--line);background:#08251c;border-radius:16px;padding:12px
  }
  .expired-meta b{display:block;font-size:13px}
  .expired-meta span{display:block;color:var(--muted);font-size:11px;margin-top:4px}
  .subscription-modal{
    position:fixed;z-index:520;left:50%;top:50%;
    width:min(560px,calc(100vw - 24px));max-height:88vh;overflow:auto;
    transform:translate(-50%,-48%) scale(.98);
    padding:20px;border-radius:24px;
    background:#071d16;border:1px solid #2b795d;
    box-shadow:0 30px 90px rgba(0,0,0,.62);
    opacity:0;pointer-events:none;transition:.2s
  }
  .subscription-modal.open{opacity:1;pointer-events:auto;transform:translate(-50%,-50%) scale(1)}
  .plan-card{
    border:1px solid var(--line);border-radius:18px;padding:15px;margin-top:12px;background:#0a291f
  }
  .plan-card.recommended{border-color:#318c68;background:#0d3327}
  .plan-price{font-size:25px;font-weight:900;margin:5px 0}
  .plan-card ul{padding-left:18px;color:var(--muted);font-size:13px;line-height:1.6}
  .subscription-note{
    margin-top:12px;padding:11px;border-radius:14px;
    background:#10251e;color:var(--muted);font-size:12px;line-height:1.5
  }

  @media(max-width:900px){
    .hero{grid-template-columns:1fr}
    .features{grid-template-columns:1fr 1fr}
    .admin-grid{grid-template-columns:1fr}
    .orbit-card{min-height:560px}
    .orbit{height:440px}
  }
  @media(max-width:560px){
    .app{padding:10px 10px 80px}
    .topbar{align-items:flex-start}
    .brand p{display:none}
    .btn{padding:9px 10px;font-size:12px}
    .orbit-card{padding:13px;min-height:520px}
    .orbit{height:405px}
    .center{width:118px;height:118px}
    .center b{font-size:14px}
    .product{width:98px;height:98px}
    .product img.product-photo{width:52px;height:52px}
    .product strong{font-size:10px}
    .product span{font-size:10px}
    .features{grid-template-columns:1fr 1fr}
    .stat-grid{grid-template-columns:1fr 1fr}
  }
</style>
</head>
<body>
<div class="app">
  <div class="topbar">
    <div class="brand">
      <div class="logo">★</div>
      <div>
        <h1>STARBUCKS MENU ORBIT</h1>
        <p>Advanced interactive ordering concept demo</p><p style="font-size:10px;opacity:.75;margin-top:2px">Independent portfolio concept — not an official Starbucks product</p>
      </div>
    </div>
    <div class="actions">
      <div class="trial-badge" id="trialBadge" aria-live="polite">🎁 Free Trial: 14d</div>
      <button class="btn ghost" id="branchBtn">📍 Main Branch</button>
      <button class="btn" id="adminBtn">⚙️ Owner</button>
      <button class="btn primary" id="cartBtn">🛒 Cart <span id="cartCount">0</span></button>
    </div>
  </div>

  <div class="hero">
    <section class="card orbit-card">
      <div class="section-title">
        <div><h2>Explore the Orbit</h2><small>Green Orbit • Tap a circle to open a 3D product popup</small><br><small style="opacity:.65">Uses real product pictures in the circles and popup.</small></div>
        <button class="btn" id="promoBtn">✨ Promo</button>
      </div>
      <div class="category-row" id="categories"></div>
      <div class="orbit" id="orbit">
        <div class="center"><div><b>STARBUCKS<br>MENU ORBIT</b><span>tap a circle to open the product in 3D</span></div></div>

        <div class="orbit-reveal" id="orbitReveal" aria-live="polite">
          <button class="btn reveal-back" id="revealBack" type="button">← Back to Orbit</button>
          <div class="reveal-actions">
            <button class="btn" id="revealCustomize" type="button">Customize</button>
            <button class="btn primary" id="revealAdd" type="button">Add to Cart</button>
          </div>

          <div class="reveal-stage">
  