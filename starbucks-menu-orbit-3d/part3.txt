acity:1;pointer-events:auto}

  .product-hero.reveal-3d .flat-preview{
    opacity:0;
    transform:translate(-50%,-50%) rotateY(-78deg) scale(.84);
  }
  .product-hero.reveal-3d .product-viewer{
    opacity:1;
    transform:scale(1);
  }
  .product-hero.reveal-3d .reveal-price{
    opacity:1;
    transform:translateY(0);
  }
  .flat-preview{
    display:none;
  }
  .flat-preview img{
    width:100%;height:154px;object-fit:cover;display:block;
  }
  .flat-preview .flat-info{
    padding:11px 12px;
  }
  .flat-preview .flat-title{
    font-size:13px;font-weight:800;line-height:1.2;
  }
  .flat-preview .flat-sub{
    margin-top:6px;font-size:11px;color:var(--muted);
  }
  .reveal-price{
    display:none;
  }

  .product-hero{
    height:320px;border-radius:20px;position:relative;overflow:hidden;
    background:
      radial-gradient(circle at 50% 22%,rgba(19,91,64,.52),rgba(8,42,32,.95) 64%,#03130e),
      linear-gradient(180deg,#0a291f,#071d16);
    perspective:1100px;
  }
  .product-hero:after{
    content:"";position:absolute;left:50%;bottom:18px;width:180px;height:28px;
    transform:translateX(-50%);
    border-radius:50%;
    background:radial-gradient(ellipse at center,rgba(0,0,0,.42),rgba(0,0,0,0) 72%);
    filter:blur(5px);
    pointer-events:none;
  }
  .viewer-hint{
    position:absolute;top:10px;right:10px;z-index:3;
    font-size:11px;padding:6px 8px;border-radius:999px;
    background:rgba(255,255,255,.08);border:1px solid rgba(212,233,226,.18);color:#d4e9e2
  }
  .product-viewer{
    position:absolute;inset:0;
    display:grid;place-items:center;
    transform-style:preserve-3d;
    cursor:grab;touch-action:none;
    opacity:1;
    transform:scale(1);
    transition:transform .18s ease-out;
  }
  .product-viewer.dragging{cursor:grabbing}
  .real-photo-model{
    position:relative;
    width:min(76%,250px);height:250px;
    transform-style:preserve-3d;
    will-change:transform;
    transition:transform .18s ease-out;
  }
  .real-photo-model.pop-in{
    animation:photoPop3D .75s cubic-bezier(.2,.85,.2,1);
  }
  @keyframes photoPop3D{
    0%{
      transform:rotateX(76deg) rotateY(0deg) scale(.72) translateY(48px);
    }
    55%{
      transform:rotateX(2deg) rotateY(-24deg) scale(1.03) translateY(-4px);
    }
    100%{
      transform:rotateX(10deg) rotateY(-18deg) scale(1) translateY(0);
    }
  }
  .photo-card{
    position:absolute;inset:0;
    border-radius:28px;
    overflow:hidden;
    background:linear-gradient(180deg,#ffffff,#e9eceb 100%);
    border:1px solid rgba(255,255,255,.22);
    box-shadow:
      0 30px 60px rgba(0,0,0,.34),
      inset 0 0 0 1px rgba(255,255,255,.18);
    transform:translateZ(34px);
  }
  .photo-card img{
    width:100%;height:100%;object-fit:cover;display:block;
  }
  .photo-card:after{
    content:"";position:absolute;inset:0;
    background:linear-gradient(135deg,rgba(255,255,255,.24),rgba(255,255,255,0) 38%,rgba(0,0,0,.13) 100%);
    pointer-events:none;
  }
  .photo-side-left,.photo-side-right,.photo-side-top,.photo-side-bottom{
    position:absolute;
    background:linear-gradient(180deg,#d5ddda,#9ca7a2 100%);
    box-shadow:inset 0 0 0 1px rgba(255,255,255,.08);
  }
  .photo-side-left,.photo-side-right{
    top:10px;bottom:10px;width:34px;border-radius:16px;
  }
  .photo-side-left{
    left:-3px;
    transform:rotateY(-90deg) translateZ(17px);
    transform-origin:left center;
  }
  .photo-side-right{
    right:-3px;
    transform:rotateY(90deg) translateZ(17px);
    transform-origin:right center;
  }
  .photo-side-top,.photo-side-bottom{
    left:10px;right:10px;height:34px;border-radius:16px;
  }
  .photo-side-top{
    top:-3px;
    transform:rotateX(90deg) translateZ(17px);
    transform-origin:center top;
  }
  .photo-side-bottom{
    bottom:-3px;
    transform:rotateX(-90deg) translateZ(17px);
    transform-origin:center bottom;
  }
  .photo-back{
  