<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Starbucks Menu Orbit — Green Orbit 3D Popup</title>
<style>
  :root{
    --bg:#061f17;
    --panel:#0b2b21;
    --panel2:#12392c;
    --text:#f5fff9;
    --muted:#b8d2c5;
    --accent:#00754a;
    --accent2:#d4e9e2;
    --line:#1e4b3a;
    --ok:#8bc79b;
    --warn:#f4c66b;
    --danger:#ef8f8f;
    --shadow:0 18px 55px rgba(0,0,0,.38);
  }
  *{box-sizing:border-box}
  body{
    margin:0;
    min-height:100vh;
    background:
      radial-gradient(circle at 50% 10%, rgba(200,134,76,.18), transparent 34%),
      linear-gradient(160deg,#041812,#09251c 55%,#041812);
    color:var(--text);
    font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
  }
  button,input,select{font:inherit}
  button{cursor:pointer}
  .app{max-width:1180px;margin:0 auto;padding:18px 16px 90px}
  .topbar{
    position:sticky;top:0;z-index:30;
    display:flex;gap:12px;align-items:center;justify-content:space-between;
    padding:12px 0;
    backdrop-filter:blur(18px);
    background:linear-gradient(to bottom,rgba(15,12,10,.95),rgba(15,12,10,.72),transparent);
  }
  .brand{display:flex;align-items:center;gap:12px}
  .logo{
    width:46px;height:46px;border-radius:50%;
    display:grid;place-items:center;
    background:linear-gradient(145deg,#c8864c,#005b3a);
    box-shadow:0 0 0 3px rgba(234,183,126,.15);
    font-weight:900;color:#f5fff9;
  }
  .brand h1{font-size:16px;margin:0;letter-spacing:.08em}
  .brand p{font-size:12px;margin:3px 0 0;color:var(--muted)}
  .actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}
  .btn{
    border:1px solid var(--line);
    background:#0d3327;
    color:var(--text);
    border-radius:14px;
    padding:10px 13px;
  }
  .btn.primary{
    background:linear-gradient(135deg,var(--accent),var(--accent2));
    color:#f5fff9;border:none;font-weight:800;
  }
  .btn.ghost{background:transparent}
  .hero{
    display:grid;grid-template-columns:1.35fr .65fr;gap:16px;margin-top:8px
  }
  .card{
    background:linear-gradient(180deg,rgba(34,24,19,.95),rgba(23,17,14,.95));
    border:1px solid var(--line);border-radius:24px;box-shadow:var(--shadow)
  }
  .orbit-card{min-height:590px;padding:18px;position:relative;overflow:hidden}
  .orbit-card:before{
    content:"";position:absolute;inset:10% 14%;pointer-events:none;
    border:1px dashed rgba(234,183,126,.18);border-radius:50%;
    transform:rotate(-8deg)
  }
  .section-title{display:flex;align-items:flex-end;justify-content:space-between;gap:12px}
  .section-title h2{margin:0;font-size:20px}
  .section-title small{color:var(--muted)}
  .category-row{display:flex;gap:8px;overflow:auto;padding:14px 0 8px}
  .chip{
    white-space:nowrap;border:1px solid var(--line);background:#0b2b21;color:var(--muted);
    padding:9px 12px;border-radius:999px
  }
  .chip.active{background:#124b38;color:#fff;border-color:#1b6f52}
  .orbit{
    position:relative;height:470px;margin-top:4px;touch-action:none;user-select:none;
    border-radius:28px;
    background:
      radial-gradient(circle at 50% 45%, rgba(32,132,92,.42) 0%, rgba(17,86,60,.34) 36%, rgba(8,42,32,.92) 72%, rgba(3,19,14,.98) 100%);
    border:1px solid rgba(212,233,226,.14);
    box-shadow: inset 0 0 0 1px rgba(255,255,255,.03), inset 0 28px 80px rgba(255,255,255,.03);
  }
  .center{
    position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);
    width:150px;height:150px;border-radius:50%;
    display:grid;place-items:center;text-align:center;
    background:
      radial-gradient(circle at 35% 30%,#0d4b37,#082a20 64%,#03130e);
    border:2px solid #1f8a63;
    box-shadow:0 0 0 10px rgba(200,134,76,.06),0 22px 45px rgba(0,0,0,.35);
    z-index:2
  }
  .center b{font-size:18px;letter-spacing:.08em}
  .center span{display:block;font-size:11px;color:#d5b99f;margin-top:6px}
  .product{
    position:absolute;
    width:124px;height:124px;border-radius:50%;
    transform:translate(-50%,-50%);
    border:1px solid #1f6e52;
    background:radial-gradient(circle at 35% 25%,#0e5e43,#0a3326 60%,#041710);
    box-shadow:0 14px 30px rgba(0,0,0,.35);
    display:flex;flex-direction:column;align-items:center;justify-content:center;
    padding:9px;text-align:center;
    transition:left .35s ease,top .35s ease,transform .2s ease,opacity .25s;
    z-index:5;pointer-events:auto;touch-action:manipulation;
  }
  .product:hover{transform:translate(-50%,-50%) scale(1.05)}
  .product .emoji{display:none}
  .product img.product-photo{
    width:64px;height:64px;border-radius:50%;object-fit:cover;
    border:2px solid rgba(255,255,255,.18);box-shadow:0 8px 18px rgba(0,0,0,.28);
    margin-bottom:5px;
  }
  .product strong{font-size:10px;line-height:1.1;max-width:92px}
  .product span{display:none}
  .product.sold{opacity:.38;filter:grayscale(1)}
  .sold-badge{
    position:absolute;top:7px;right:7px;font-size:9px;background:#5b2828;
    padding:3px 6px;border-radius:999px
  }
  .side{padding:18px;display:flex;flex-direction:column;gap:14px}
  .stat-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}
  .stat{background:#09251c;border:1px solid var(--line);border-radius:18px;padding:14px}
  .stat b{font-size:22px}
  .stat span{display:block;font-size:11px;color:var(--muted);margin-top:4px}
  .promo{
    padding:15px;border-radius:18px;
    background:linear-gradient(135deg,rgba(200,134,76,.18),rgba(234,183,126,.06));
    border:1px solid rgba(234,183,126,.25)
  }
  .promo h3{margin:0 0 6px;font-size:15px}
  .promo p{margin:0;color:var(--muted);font-size:13px}
  .mini-list{display:grid;gap:9px}
  .mini{
    display:flex;align-items:center;justify-content:space-between;gap:10px;
    background:#09251c;border:1px solid var(--line);border-radius:14px;padding:10px 12px
  }
  .mini small{color:var(--muted)}
  .features{
    display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-top:16px
  }
  .feature{
    padding:14px;border-radius:18px;background:#0a271e;border:1px solid var(--line)
  }
  .feature b{display:block;font-size:13px;margin-bottom:4px}
  .feature span{font-size:11px;color:var(--muted)}
  .drawer,.modal{
    position:fixed;z-index:100;background:#16100c;border:1px solid var(--line);
    box-shadow:0 25px 70px rgba(0,0,0,.6)
  }
  .drawer{
    right:12px;top:12px;bottom:12px;width:min(420px,calc(100vw - 24px));
    border-radius:24px;padding:18px;transform:translateX(110%);transition:.28s ease;
    overflow:auto
  }
  .drawer.open{transform:translateX(0)}
  .drawer-head{display:flex;align-items:center;justify-content:space-between;gap:8px}
  .drawer h3{margin:0}
  .icon-btn{border:1px solid var(--line);background:#0d3327;color:#fff;border-radius:12px;padding:8px 11px}
  .cart-item{
    padding:12px 0;border-bottom:1px solid var(--line);
    display:grid;grid-template-columns:1fr auto;gap:8px
  }
  .cart-item small{color:var(--muted)}
  .total{display:flex;justify-content:space-between;font-size:18px;font-weight:800;margin:16px 0}
  .field{display:grid;gap:6px;margin:10px 0}
  .field label{font-size:12px;color:var(--muted)}
  .field input,.field select{
    width:100%;background:#041812;color:#fff;border:1px solid var(--line);
    border-radius:12px;padding:11px 12px
  }
  .modal{
    left:50%;top:50%;transform:translate(-50%,-48%) scale(.98);
    width:min(560px,calc(100vw - 24px));max-height:88vh;overflow:auto;
    border-radius:24px;padding:18px;opacity:0;pointer-events:none;transition:.2s
  }
  .modal.open{opacity:1;pointer-events:auto;transform:translate(-50%,-50%) scale(1)}
  .backdrop{
    position:fixed;inset:0;background:rgba(0,0,0,.58);z-index:90;
    opacity:0;pointer-events:none;transition:.2s
  }
  .backdrop.open{opacity:1;pointer-events:auto}

  .product-hero.reveal-3d .flat-preview{
    opacity:0;
    transform:translate(-50%,-50%) rotateY(-78deg) scale(.84);
  }
  .product-hero.reveal-3d .product-viewer{
    opacity:1;
    transform:scale(1);
  }
  .product-hero.reveal-3d .reveal-price{
    opacity:1;
    transform:translateY(0);
  }
  .flat-preview{
    display:none;
  }
  .flat-preview img{
    width:100%;height:154px;object-fit:cover;display:block;
  }
  .flat-preview .flat-info{
    padding:11px 12px;
  }
  .flat-preview .flat-title{
    font-size:13px;font-weight:800;line-height:1.2;
  }
  .flat-preview .flat-sub{
    margin-top:6px;font-size:11px;color:var(--muted);
  }
  .reveal-price{
    display:none;
  }

  .product-hero{
    height:300px;border-radius:20px;position:relative;overflow:hidden;
    background:
      radial-gradient(circle at 50% 25%,rgba(13,95,67,.75),rgba(8,42,32,.96) 64%,#03130e),
      linear-gradient(180deg,#0a291f,#071d16);
    perspective:1000px;
  }
  .product-hero:after{
    content:"";position:absolute;inset:auto 12% 18px 12%;height:26px;
    border-radius:50%;
    background:radial-gradient(ellipse at center,rgba(0,0,0,.38),rgba(0,0,0,0) 72%);
    pointer-events:none;
  }
  .viewer-hint{
    position:a